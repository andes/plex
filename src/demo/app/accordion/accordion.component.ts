import { Component } from '@angular/core';

@Component({
    templateUrl: 'accordion.html',
})
export class AccordionDemoComponent {
    public test = false;

    toggle(showed) {
    }

    bool(active) {
    }

}
