import { Component } from '@angular/core';

@Component({
  templateUrl: 'accordion.html',
})
export class AccordionDemoComponent {
  private test = false;

  toggle(showed) {
  }

  bool(active) {
  }

}
